# 运维管理功能指南

## 概述

v2.4.0 新增了完善的运维管理功能，包括连通性测试、健康检查、日志管理和流量统计。

## 功能列表

### 1. 连通性测试 🧪

**功能**: 自动测试代理是否正常工作

**使用方法**:
```bash
# 命令行
./xray-http-proxy.sh --test-connectivity

# 交互菜单
选择 "12. 🧪 连通性测试"
```

**测试内容**:
- HTTP 代理连接测试
- SOCKS5 代理连接测试
- 访问外部 API 验证

**输出示例**:
```
🧪 代理连通性测试
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[INFO] 测试 HTTP 代理 (127.0.0.1:13053)

[SUCCESS] ✓ HTTP 代理连接成功

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[INFO] 测试 SOCKS5 代理 (127.0.0.1:14052)

[SUCCESS] ✓ SOCKS5 代理连接成功

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[SUCCESS] 🎉 所有测试通过 (2/2)
```

---

### 2. 系统健康检查 🏥

**功能**: 全面检查系统状态，快速定位问题

**使用方法**:
```bash
# 命令行
./xray-http-proxy.sh --health-check

# 交互菜单
选择 "13. 🏥 系统健康检查"
```

**检查项目**:
1. Xray 安装状态
2. 配置文件有效性
3. 服务运行状态
4. 端口监听状态
5. 日志文件状态
6. 代理连通性

**使用场景**:
- 服务启动后验证
- 故障排查
- 定期巡检
- 部署验证

**输出示例**:
```
🏥 系统健康检查

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[INFO] [1/6] 检查 Xray 安装
[SUCCESS] ✓ Xray 已安装

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[INFO] [2/6] 检查配置文件
[SUCCESS] ✓ 配置文件有效

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[INFO] [3/6] 检查服务状态
[SUCCESS] ✓ 服务正在运行

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[INFO] [4/6] 检查端口监听
[SUCCESS] ✓ HTTP 端口 13053 正在监听
[SUCCESS] ✓ SOCKS5 端口 14052 正在监听

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[INFO] [5/6] 检查日志文件
[SUCCESS] ✓ 日志文件存在 (大小: 36K)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[INFO] [6/6] 连通性测试
[SUCCESS] ✓ 代理连接正常

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[SUCCESS] 🎉 健康检查通过，系统运行正常！
```

---

### 3. 日志管理 📜

**功能**: 查看、搜索、清理日志

**使用方法**:
```bash
# 命令行
./xray-http-proxy.sh --logs

# 交互菜单
选择 "14. 📜 日志管理"
```

**功能选项**:
1. 查看最后 50 行
2. 查看最后 100 行
3. 实时监控日志（tail -f）
4. 搜索日志内容

**使用场景**:
- 查看最近活动
- 排查错误
- 监控实时流量
- 搜索特定事件

**示例**:
```bash
# 实时监控
./xray-http-proxy.sh --logs
# 选择 "3) 实时监控日志"
# 按 Ctrl+C 退出

# 搜索错误
./xray-http-proxy.sh --logs
# 选择 "4) 搜索日志"
# 输入: error
```

---

### 4. 日志清理 🧹

**功能**: 清理日志文件，防止占满磁盘

**使用方法**:
```bash
# 命令行
./xray-http-proxy.sh --clean-logs

# 交互菜单
选择 "15. 🧹 清理日志"
```

**清理方式**:
1. **清空日志** - 完全删除日志内容
2. **保留最后 1000 行** - 只保留最近记录
3. **备份并清空** - 备份后清空

**自动轮转**:
- 日志超过 50MB 自动轮转
- 保留最近 5 个备份
- 自动删除旧备份

**示例**:
```bash
# 备份并清空
./xray-http-proxy.sh --clean-logs
# 选择 "3) 备份并清空"

# 备份文件格式
xray-proxy.log.20251204_073000.bak
```

---

### 5. 流量统计 📊

**功能**: 统计代理使用情况

**使用方法**:
```bash
# 命令行
./xray-http-proxy.sh --stats

# 交互菜单
选择 "16. 📊 流量统计"
```

**统计内容**:
- 总连接数
- 错误次数
- 最近活动时间
- 最近 10 条连接记录

**输出示例**:
```
📊 流量统计

[INFO] 分析日志文件: xray-proxy.log

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  总连接数: 1234
  错误次数: 5
  最近活动: 2025-12-04 07:30:15
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[INFO] 最近 10 条连接记录:

2025-12-04 07:30:15 tcp:443 accepted
2025-12-04 07:30:10 tcp:80 accepted
...
```

---

## 运维最佳实践

### 日常巡检

**每日检查**:
```bash
# 1. 健康检查
./xray-http-proxy.sh --health-check

# 2. 查看流量统计
./xray-http-proxy.sh --stats

# 3. 检查日志大小
du -h xray-proxy.log
```

### 故障排查流程

**步骤 1: 健康检查**
```bash
./xray-http-proxy.sh --health-check
```

**步骤 2: 查看日志**
```bash
./xray-http-proxy.sh --logs
# 选择 "1) 查看最后 50 行"
```

**步骤 3: 连通性测试**
```bash
./xray-http-proxy.sh --test-connectivity
```

**步骤 4: 重启服务**
```bash
./xray-http-proxy.sh --restart
```

### 日志管理策略

**定期清理**:
```bash
# 每周清理一次
./xray-http-proxy.sh --clean-logs
# 选择 "3) 备份并清空"
```

**自动化脚本**:
```bash
# 添加到 crontab
# 每周日凌晨 2 点清理日志
0 2 * * 0 cd /path/to/xray-http-proxy && ./xray-http-proxy.sh --clean-logs
```

### 监控告警

**检查脚本示例**:
```bash
#!/bin/bash
# monitor.sh - 监控脚本

cd /path/to/xray-http-proxy

# 健康检查
if ! ./xray-http-proxy.sh --health-check >/dev/null 2>&1; then
    echo "警告: 健康检查失败" | mail -s "Xray 代理告警" admin@example.com
fi

# 检查日志大小
log_size=$(du -m xray-proxy.log | cut -f1)
if [ "$log_size" -gt 100 ]; then
    echo "警告: 日志文件过大 (${log_size}MB)" | mail -s "Xray 日志告警" admin@example.com
fi
```

---

## 命令速查表

| 功能 | 命令 | 菜单 |
|------|------|------|
| 连通性测试 | `--test-connectivity` | 12 |
| 健康检查 | `--health-check` | 13 |
| 日志管理 | `--logs` | 14 |
| 清理日志 | `--clean-logs` | 15 |
| 流量统计 | `--stats` | 16 |

---

## 常见问题

**Q: 健康检查失败怎么办？**
A: 按照检查输出的错误信息逐项排查，通常是服务未启动或配置错误。

**Q: 日志文件太大怎么办？**
A: 使用日志清理功能，选择"备份并清空"，或启用自动轮转。

**Q: 连通性测试失败？**
A: 检查服务是否运行、端口是否监听、防火墙规则是否正确。

**Q: 流量统计不准确？**
A: 统计基于日志文件，确保日志级别设置正确且未被清空。

---

## 更多信息

- [README.md](README.md) - 项目主文档
- [SOCKS5_GUIDE.md](SOCKS5_GUIDE.md) - SOCKS5 使用指南
- [CHANGELOG.md](CHANGELOG.md) - 版本更新日志
